# Python dependencies for Resume Parser

# Core FastAPI
fastapi==0.115.12
uvicorn[standard]==0.34.0
pydantic==2.10.6
pydantic-settings==2.7.0
python-multipart==0.0.9
email-validator==2.2.0

# DeepSeek-OCR-2 Dependencies
# Note: PyTorch will be installed with CUDA 12.8 via start.bat
# torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
transformers==4.46.3
huggingface-hub==0.28.1
accelerate==1.2.1
einops==0.8.1
timm==1.0.12
Pillow>=11.2.0
addict==2.4.0
easydict==1.13
matplotlib==3.10.0
safetensors==0.5.2

# PDF Processing
PyMuPDF==1.25.5
pdf2image==1.17.0

# vLLM for DeepSeek-OCR-2 (optional, for batch processing)
# Note: Will be installed via specific wheel URL if needed
# vllm==0.8.5

# Flash Attention 2 (recommended for DeepSeek-OCR-2 performance)
# Note: Requires CUDA. Install separately: pip install flash-attn==2.7.3 --no-build-isolation
# flash-attn==2.7.3

# LOCAL LLM Parser (100% local, no data sent to cloud)
ollama==0.4.4

# Utilities
python-dotenv==1.0.1
loguru==0.7.2
psutil==5.9.8
pynvml==11.5.0

# Image preprocessing
opencv-python==4.10.0.84

# Note: Install using uv for 10x faster installation:
# uv pip install -r requirements.txt
